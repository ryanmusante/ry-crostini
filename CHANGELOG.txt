CHANGELOG
=========

crostini-setup-duet5.sh — Crostini post-install bootstrap
Lenovo IdeaPad Duet 5 Chromebook (82QS0001US / 13Q7C6)

Note: Multiple versions on the same date reflect iterative
development and review within single sessions. Each entry
represents a discrete, committed change set.


3.3.5  2026-03-16

  * add SND_DEV_COUNT to step 7 unset list (was leaking until step 20)
  * full Crostini/Debian Bookworm bash compatibility audit: all bashisms
    confirmed bash 4.0+ safe (Bookworm ships 5.2); all pre-step-4
    commands verified present in Crostini base image; command dependency
    ordering (gpg→step 4→step 11, locale-gen→step 4→step 15) confirmed
    enforced by checkpoint system
  * bump version 3.3.4 → 3.3.5; all cross-refs aligned

3.3.4  2026-03-16

  * fix CRITICAL: revert broken SSH_ASKPASS approach for ssh-keygen
    passphrase delivery — ssh-keygen does NOT honor SSH_ASKPASS (only
    ssh and ssh-add do); reverted to -N flag with documented /proc
    argv exposure limitation and immediate variable zeroing
  * add VS Code .deb integrity check: dpkg-deb --info validation
    before dpkg -i to catch corrupt or truncated downloads
  * remove dead _askpass_helper variable from cleanup trap
  * bump version 3.3.3 → 3.3.4; all cross-refs aligned

3.3.3  2026-03-16

  * add unset for 6 remaining leaked variables: CORE_PKGS (step 4),
    DEV_PKGS (step 5), GPU_STABLE_PKGS + GPU_VOLATILE_PKGS (step 6),
    AUDIO_PKGS (step 7), GUI_PKGS (step 9), _VSCODE_DEB (step 14)
  * all 55 non-local non-readonly variables now confirmed unset at
    step boundaries

3.3.2  2026-03-15

  * add unset for 20 leaked variables across steps 1, 2, 6, 7, 8,
    11, 14, 15, 18 (CURRENT_ARCH, AVAIL_KB, AVAIL_MB, AVAIL_MB_NOW,
    GPU_ENV_FILE, AUDIO_ENV_FILE, PA_CLIENT, SOMMELIER_ENV,
    GTK3_SETTINGS, GTK4_SETTINGS, GTK2_RC, QT_ENV, XRESOURCES,
    FC_LOCAL, CURSOR_DIR, NPM_GLOBAL, VSCODE_FLAGS, PROFILE_D,
    MEM_CONF, SSH_KEY)

3.3.1  2026-03-15

  * fix SC2015: replace A && B || C with if/else for dosbox, scummvm
    version checks (step 17) and log file creation (init)
  * fix SC2024: add shellcheck disable for gpg dearmor redirect —
    false positive; redirect captures log messages, not -o output
  * fix header Usage: comment missing --minimal, --from-step, --verify,
    --reset, --git-name, --git-email, --ssh-comment flags
  * move all inline comments to above-section comments (14 instances:
    wait flush, cat stdin, libgl1, pavucontrol, GUI_PKGS array)
  * README: add --ssh-comment and --minimal to Usage block; add
    --minimal to Features section

3.3.0  2026-03-15

  * fix step 20: verification output (tools, configs, GPU, audio,
    display, reminders) now written to log file via logprintf
    helper that strips ANSI escapes for the log copy
  * fix check_tool: stderr no longer leaks into version string
    (2>&1 → 2>/dev/null)
  * fix 8 version-report lines: stderr contaminated output for
    python, pip, node, npm, git, vscode, dosbox, scummvm
    (all changed to 2>/dev/null)
  * fix 13 unconditional log calls after dry-run'd run() that
    falsely claimed success: symlinks, xresources, firefox,
    thunar, evince, eog, desktop dir, npm prefix, XDG dirs,
    flatpak — all guarded with $DRY_RUN check

3.2.0  2026-03-15

  * trim all multiline comments to single lines (6 blocks)
  * fix --dry-run: arch check now warns instead of dying on
    non-ARM64 hosts (was: --dry-run unusable off-device)
  * fix --dry-run: root check now warns instead of dying
  * fix step 12 --dry-run: wrap rustup download-execute in
    explicit dry-run guard (was: mktemp created real tmpfile,
    empty-file check died because curl was dry-run'd)
  * fix step 8 --dry-run: suppress "Font cache rebuilt" log
    when fc-cache was not actually executed
  * fix step 15 --dry-run: suppress "inotify watchers applied"
    and "locale generated" logs when commands were not executed
  * fix step 12: suppress shell error in rustc/cargo version
    output (2>&1 → 2>/dev/null)
  * remove redundant profile.d log (write_file_sudo already logs)

3.1.0  2026-03-15

  * fix run()/run_shell(): add wait after process substitution tee
    to flush log output before next command (#1)
  * fix --from-step/--verify: defer checkpoint write until after
    lock acquisition to prevent corruption on concurrent invocation (#2)
  * fix PID write: atomic tmpfile+mv instead of direct echo (#3)
  * fix step 12 rustup: download to tmpfile then execute, preventing
    truncated script execution on partial curl failure (#4)
  * fix step 6: unset GL_VENDOR/GL_RENDERER/GL_VERSION at end of
    step instead of leaking until step 20 (#5)
  * fix step 11 NodeSource: verify sources.list content after write
    before apt-get update (#6)
  * fix step 15 locale.gen: restore backup on sed -i failure (#7)
  * add _rustup_tmp cleanup to trap handler
  * add --minimal flag: skip heavy optional packages
    (gnome-disk-utility) for RAM-constrained devices (#12)
  * add date compression note to CHANGELOG header (#9)

3.0.2  2026-03-15

  * fix lock file: handle missing/empty PID file as stale lock (crash
    between mkdir and PID write left unrecoverable lock on next run)
  * fix early init: add die guards to date and id -u command
    substitutions; die on log file creation failure
  * fix cleanup trap: guard rm -rf with [[ -n ]] (defensive against
    empty LOCK_FILE)
  * fix step 14 VS Code curl: preserve error message instead of
    swallowing with || true (download failures now warn with context)
  * fix step 20 verification: cache glxinfo and vulkaninfo output in
    variables (was: 3+2 redundant subprocess invocations)
  * fix step 16 Flathub URL: use canonical dl.flathub.org/repo/ path
    (was: flathub.org/repo/ which redirects)
  * add --from-step=N flag: start (or restart) from a specific step
  * add --verify flag: run only step 20 (summary and verification)
  * update help text with new flags

3.0.1  2026-03-15

  * fix run()/run_shell() stderr merge: use separate process substitution
    per fd so stderr remains on fd 2 (was: 2>&1 before tee merged both
    onto stdout); document async flush as known limitation
  * fix step 14 VS Code dpkg/apt-get chain: restructure as if/elif/else
    so both dpkg and apt-get failures are reported (was: || chain only
    surfaced the apt-get failure)
  * fix step 11 NodeSource GPG dearmor: atomic tmpfile+mv for keyring
    (was: direct write to final path)
  * fix step 15 locale.gen: backup before sed -i
  * fix cleanup trap: add _ns_key and _ns_gpg tmpfile removal (step 11
    leak on mid-step failure)
  * remove stale unset DEBIAN_FRONTEND from step 17 (contradicts the
    global export at script top added in 2.7.1 to persist across
    checkpoint resume; var dies with process at exit 0)
  * fix step 20 cleanup comment: "step 19" → "step 20" (stale from
    3.0.0 renumbering)
  * split step 6 GPU packages into stable batch + volatile per-package
    loop (reduces apt-get calls from 20 to 6 for known-stable names)
  * add run_shell() comment: only called with hardcoded strings, never
    user input (clarifies "No eval" claim)
  * README: clarify "No eval" bullet re: run_shell() bash -c usage
  * crostini-gaming-packages.txt: add TOFU warning for box86/box64
    third-party repos (ryanfortner.github.io)

3.0.0  2026-03-15

  * add step 17: gaming packages (DOSBox, ScummVM, RetroArch)
    - dosbox and scummvm via apt-get (Debian Bookworm arm64 repos)
    - RetroArch via Flatpak (aarch64 confirmed on Flathub)
  * renumber subsequent steps (SSH → 18, backup → 19, summary → 20)
  * move unset DEBIAN_FRONTEND from Flatpak step to gaming step
  * add dosbox, scummvm, RetroArch to summary verification
  * add gaming reference pointer to summary reminders
  * Phase 3 (box86/box64/Wine) and Phase 4 (Heroic) remain manual;
    documented in crostini-gaming-packages.txt
  * README: update step table (20 steps)

2.9.0  2026-03-15

  * fix run()/run_shell() exit code propagation: replace pipe + || true
    with process substitution (> >(tee ...)) so callers receive the real
    exit code; previous pattern reset PIPESTATUS via || true, silently
    masking all command failures and suppressing warnings
  * fix dpkg/apt-fix chain in step 14 (now works correctly with real
    exit codes from run())
  * remove DEBIAN_FRONTEND unset from step 16 (prevents resume fragility
    when checkpoint lands after the unset but before a later apt call)
  * fix NodeSource GPG key: download to tmpfile before dearmor to prevent
    truncated keyring from partial curl pipe failure
  * add GDK_BACKEND comment noting Electron app caveats (Slack, Discord)
  * add box64/box86 community x86 translation mention to header, step 19
    reminders, and README limitations
  * remove unused netcat-openbsd from CLI tools (step 4)
  * add RAM-constrained note to gnome-disk-utility comment (step 9)
  * add non-atomic note to sed -i locale.gen (step 15)

2.8.0  2026-03-15

  * default to fully unattended execution (zero interactive prompts)
  * add --interactive flag to restore original prompt behavior
  * add --git-name=, --git-email=, --ssh-comment= flags
  * wrap step 2 + step 18 interactive blocks (open_chromeos_url, sleep,
    read, instruction printf) inside if ! $UNATTENDED guards
  * SSH key auto-generated with no passphrase in unattended mode
  * update help text, banners, README for --interactive semantics

2.7.1  2026-03-15

  * move DEBIAN_FRONTEND=noninteractive before step blocks (persist on
    checkpoint resume)
  * cosmetic: strip trailing whitespace, normalize blank lines

2.7.0  2026-03-15

  * call ssh-keygen directly to avoid logging passphrase via run()
  * initialize LOG_FILE with chmod 600 before first write
  * add PID-based stale lock detection (auto-remove dead locks)
  * apply tee pattern to run_shell() (show rustup/NodeSource progress)
  * use sudo mktemp in write_file_sudo() for atomic writes
  * clean up variables at end of steps 1, 2, 13, 17, 19

2.6.0  2026-03-15

  * add SSH passphrase prompt (remove hardcoded -N "")
  * guard 6 git config keys with existing-value checks
  * conditionalize inotify sysctl success/fail log
  * replace all 18 'sudo apt' with 'sudo apt-get'
  * export DEBIAN_FRONTEND=noninteractive around apt steps
  * make write_file() and write_file_sudo() atomic (tmpfile + mv)
  * add apt-get full-upgrade in step 3
  * hoist check_tool()/check_config() to helpers section
  * tee run() output to terminal and log (fix silent-freeze UX)
  * move NODE_MAJOR, SYSCTL_CONF to constants block
  * rename SND_DEVICES -> SND_DEV_COUNT for consistency

2.5.10  2026-03-15

  * guard code --version pipe against SIGPIPE under pipefail

2.5.9  2026-03-15

  * guard check_tool() --version pipe against SIGPIPE
  * sync all 19 step descriptions across help, banners, README

2.5.8  2026-03-15

  * remove all box-drawing decorative characters
  * remove unused CYAN color variable

2.5.7  2026-03-15

  * trim multiline comments to single lines
  * strip decorative line art from section headers
  * inline GTK3_DIR and GTK4_DIR (used once each)

2.5.6  2026-03-15

  * fix step 8 comment: add GTK2 and cursor to listed layers
  * fix GPU comment: Crostini has X11 via sommelier XWayland
  * add || true guard to xdg-user-dirs-update

2.5.5  2026-03-15

  * add || true to rmdir in cleanup() trap (fix exit code corruption)
  * add || true to VS Code curl download (graceful fallback)
  * restructure VS Code dpkg/apt-fix chain as if/else

2.5.4  2026-03-15

  * add || warn to git lfs install
  * add || true to SSH pubkey tee

2.5.3  2026-03-15

  * fix check_tool "glmark2" -> "glmark2-es2-wayland"
  * validate checkpoint file content is numeric

2.5.2  2026-03-15

  * add || die guards to write_file()/write_file_sudo()
  * replace deprecated VS Code Ozone flags with --ozone-platform-hint=auto
  * add -s check on NodeSource GPG keyring after curl|gpg
  * remove glmark2 GLX variant (Crostini is Wayland-only)

2.5.1  2026-03-14

  * wrap VS Code download in DRY_RUN guard
  * replace fragile find -not -name with -mindepth 1
  * add -u (nounset) to run_shell() subshell

2.5.0  2026-03-13

  * guard all step 2/13/17/18 interactive prompts with DRY_RUN
  * replace hardcoded vscode temp path with mktemp
  * add error handling to all unguarded sudo calls

2.4.1  2026-03-09

  * add existence guards to 5 write_file calls (prevent overwrite)
  * make NODE_MAJOR and SYSCTL_CONF readonly
  * replace ls|wc with find for /dev/snd count (SC2012)
  * wrap preflight curl in DRY_RUN guard

2.4.0  2026-03-08

  * add mkdir-based lock file for concurrent-run safety
  * replace hardcoded /tmp/vscode-arm64.deb with mktemp
  * fix set_checkpoint() writing during --dry-run
  * route all remaining dry-run bypasses through run()
  * fix --dry-run crash in step 17 (chmod on non-existent key)

2.3.1  2026-03-08

  * route per-package apt loops through run() for dry-run/logging
  * add vm.swappiness writability check (read-only in Crostini)
  * show virgl Vulkan status in summary

2.3.0  2026-03-08

  * remove pulseaudio daemon (conflicts with Crostini host audio)
  * remove standalone xwayland (conflicts with sommelier)
  * fix OLED font rendering: rgba=none (no LCD subpixel stripe)
  * replace ping with curl for network check (ICMP blocked)
  * add --yes to gpg --dearmor for idempotent re-runs
  * remove transitional apt-transport-https

2.2.0  2026-03-08

  * remove Fish shell step (Crostini default is bash)
  * renumber steps 18-20 -> 17-19

2.1.1  2026-03-08

  * add vm.swappiness=10 and dirty ratio tuning for 4 GB device
  * add memory config to summary verification

2.1.0  2026-03-08

  * eliminate all eval usage: run() passes "$@" directly
  * add run_shell(), write_file(), write_file_sudo() helpers
  * replace hardcoded UID 1000 with $(id -u)
  * pipefail-safe logging (no pipe in log path)
  * add trap handler for EXIT/INT/TERM

2.0.0  2026-03-08

  * full rewrite: 19-step automated setup
  * auto-open ChromeOS settings via garcon-url-handler
  * GPU + graphics stack (Mesa, Virgl, Wayland, Vulkan)
  * audio stack (ALSA, PulseAudio, GStreamer)
  * display scaling tuned for 13.3" FHD OLED
  * GUI apps with MIME defaults, fonts, screenshots
  * VS Code Wayland/Ozone flags
  * summary: GPU, Vulkan, Wayland, audio, configs, tools

1.1.0  2026-03-08

  * add GPU detection and verification
  * add GPU/Wayland/audio/shared folder status to summary

1.0.0  2026-03-08

  * initial release: Crostini bootstrap with checkpoint resume,
    --dry-run, colored output, and full logging
