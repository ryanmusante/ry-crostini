CHANGELOG
=========

crostini-setup-duet5.sh -- Crostini post-install bootstrap
Lenovo IdeaPad Duet 5 Chromebook (82QS0001US / 13Q7C6)

4.3.0  2026-03-19
  - fix step 9: remove adwaita-icon-theme-full (merged into
    adwaita-icon-theme since 45.0-4; removed in Trixie)
  - fix step 11: unconditionally refresh NodeSource GPG keyring when
    repo is configured (Trixie Sequoia/sqv rejects SHA1 binding sigs
    since 2026-02-01; NodeSource rotated to SHA-512 on 2026-01-22);
    guard existing install-path key download to avoid double download
  - add step 7: mask pipewire-pulse.socket/service (Trixie defaults to
    PipeWire; conflicts with Crostini host-forwarded PulseAudio)
  - add step 3: warn that cros.list reverts on container restart
  - add step 18: PipeWire mask verification check
  - update gpu.conf comment: GDK_BACKEND redundancy on Trixie
  - update .Xresources comment: sommelier DPI bucket limitation

4.2.0  2026-03-19
  - fix step 3: handle deb822 .sources files (Suites: field) in addition
    to legacy .list files during Trixie upgrade -- Trixie recommends the
    new format and some installs may already use it
  - add step 3d: run apt modernize-sources after upgrade to convert
    legacy .list -> deb822 .sources format (non-fatal; old format
    supported until 2029)
  - add step 14a: install linux-sysctl-defaults for Trixie ping
    permissions (net.ipv4.ping_group_range); package provides
    /usr/lib/sysctl.d/50-default.conf
  - add /tmp tmpfs note in header: Trixie mounts /tmp as tmpfs
    (RAM-backed); script downloads to /tmp are transient and small
  - renumber step 14 sub-steps (14b-14e -> 14c-14f) after new 14a

4.1.0  2026-03-19
  - add Trixie auto-upgrade in step 3: detect VERSION_CODENAME, rewrite
    /etc/apt/sources.list (bookworm -> trixie), update cros.list and any
    extra .list files; full-upgrade then performs the dist-upgrade
  - back up sources.list to /etc/apt/sources.list.pre-trixie before
    rewrite; rollback on sed failure
  - post-upgrade codename verification with actionable warning
  - dry-run traces all upgrade operations without modifying sources
  - update step 3 title in banner, usage(), section comment, README
  - update README Compatibility section with auto-upgrade documentation

4.0.0  2026-03-19
  - BREAKING: dual-target Bookworm (12) and Trixie (13) -- package arrays
    use canonical non-transitional names that resolve on both versions
  - replace GPU transitionals: libegl1-mesa -> libegl1,
    libgles2-mesa -> libgles2 (real packages on both Bookworm and Trixie;
    the -mesa transitionals are dropped from Trixie mesa source)
  - detect VERSION_CODENAME from /etc/os-release; use it for network
    preflight URL (replaces hardcoded dists/bookworm/)
  - log detected codename in step 1 preflight
  - update ALSA/GStreamer comments for Bookworm/Trixie dual state
  - README: add Compatibility section with Trixie upgrade instructions
    and t64 transition explanation; replace Trixie migration section

3.22.0  2026-03-19
  - fix _strip_log_ansi: explicit chmod 600 on tmpfile before sed writes
    (future-proofs against umask changes)
  - fix step 13 VS Code download: collapse two if-blocks into single
    if/elif/else so curl failure skips install (no contradictory UX on
    partial downloads; dpkg-deb --info was already catching corruption)
  - fix step 11 NodeSource: move unset _ns_key/_ns_gpg from mid-flow
    (L1372) to step boundary (L1400) so cleanup trap retains visibility
    if die fires during apt update/install

3.21.0  2026-03-18
  - add timeout 3 to step 18 inline node --version (consistency with
    check_tool and step 11 which all use timeout 3)
  - add timeout 5 to 2 flatpak list --app calls (steps 16, 18) to
    prevent hang on dbus/polkit issues during verification
  - fix README: add NodeSource apt repo to Config files written prose
    (check_config added in 3.16.0 but prose was not updated)

3.20.0  2026-03-18
  - fix usage() OPTIONS alignment: all descriptions now start at a
    common column
  - fix 3 en-dash (U+2013) sites in script and 8 in README -- now
    ASCII hyphen-minus per 3.14.0 normalization policy

3.19.0  2026-03-18
  - fix usage() step 2/4 vocabulary to match README/banner
  - fix CHANGELOG 3.18.0 header wording
  - add dry-run log at top of step 18 verification
  - add above-line comments on 2 unquoted heredocs ($CROS_UID)
  - quote $rc/$_tee_rc inside [[ ]] for consistency (3 sites)

3.18.0  2026-03-18
  - sync step titles across banner, section comment, usage(), README
    (steps 6-8, 11-13): add glmark2, pavucontrol, Super key, DPI 120,
    "LTS arm64 via NodeSource", "stable aarch64 via rustup"

3.17.0  2026-03-18
  - move 3 inline comments to above-line; replace 5 orphaned tracker
    refs with descriptive text; sync 18 section comments to banners
  - replace "Phase 2:" label, "=" assignment comment, inline SC2024
  - split step 17 section comment for --interactive detail

3.16.0  2026-03-18
  - fix step 3 cross-ref: sync usage()/banner to README
  - fix step 9 eog MIME: add gif, webp, svg+xml, bmp, tiff
  - fix step 18: add check_config for apt tuning and NodeSource repo

3.15.0  2026-03-18
  - add DEBIAN_FRONTEND=noninteractive to 2 apt-get update calls
  - fix step 11 dry-run log to match live command

3.14.0  2026-03-18
  - add SC2329 suppression on 3 trap-invoked functions
  - normalize CHANGELOG.txt to ASCII (em-dash, en-dash, arrow, S.)

3.13.0  2026-03-18
  - inline gaming guide into README S. Gaming
  - add TOFU comment on VS Code .deb, partial-application note on
    memory sysctl, --purge on autoremove, dosbox-staging comment
  - deduplicate VS Code warnings; add ", memory" to step 14 title

3.8.0 - 3.12.0  2026-03-16 - 2026-03-18
  Execution and safety:
  - CRITICAL: fix set -e/pipefail restore order crash in run()
  - CRITICAL: fix PIPESTATUS race -- saved atomically into local _ps
  - eliminate run_shell() (bash -c); eliminate date(1) from cleanup
  - fix cleanup() recursion, signal exit codes (128+N re-raise)
  - guard 7 pipelines against errexit; fix A&&B||C in run()
  - gate locale.gen sed -i on backup; chmod 644 before mv in writers
  Unattended / dry-run:
  - all sudo apt-get passes DEBIAN_FRONTEND explicitly
  - --force-confdef/--force-confold on upgrades; --noninteractive on
    flatpak; 8 dry-run fidelity fixes; _CHECKPOINT_OVERRIDE for
    --from-step/--verify in --dry-run; -t 300 on all 7 read prompts
  Logging and verification:
  - -r on 9 xargs calls; counters on 5 uncounted checks; fix 10+
    false success logs; single-pass ANSI strip; _prompt() for audit;
    check_tool() stderr fallback; set_checkpoint before rm in step 18
  Packages and config:
  - drop transitional gstreamer1.0-pulseaudio/explicit libasound2
  - add wl-clipboard, xterm, SOMMELIER_ACCELERATORS=Super_L
  - batch qt5; Acquire::Retries 3; Queue-Mode "access"; PATH dedup
  - NodeSource apt update failure now dies; Node major version warn
  Lock and tempfiles:
  - stale lock find -delete; || die on mktemp (steps 12, 13)
  - cleanup temp removal if/fi; GPG fingerprint split die messages
  Cross-reference and docs:
  - sync all banners/usage/README; Trixie migration note; NodeSource
    maintenance-mode comment; rustup TOFU; --help/--version in README

3.3.0 - 3.7.0  2026-03-15 - 2026-03-16
  - CRITICAL: fix cleanup deleting lock on --help/--version exit
  - CRITICAL: fix run() PIPESTATUS race (|| true reset to 0)
  - CRITICAL: revert broken SSH_ASKPASS; 3 rm -rf -> targeted rmdir
  - BREAKING: remove git/SSH step; renumber 14-20 -> 13-18
  - fix set_checkpoint() atomicity, signal coverage (HUP/PIPE/QUIT),
    --from-step/--verify conflict, logprintf() order, ANSI log leak,
    install_pkgs_best_effort() set -e safety, 10 false success logs,
    10 missing timeouts, source cargo/env, stale apt upgrade
  - add batch+fallback installer, step_banner() dual-write, parallel
    apt, verification counters, elapsed time, dpkg-deb --info, --
  - variable leak cleanup (55 vars confirmed unset at boundaries)

3.0.0 - 3.2.0  2026-03-15
  - add step 17: gaming (DOSBox, ScummVM, RetroArch)
  - fix run() exit code, --dry-run arch/root checks, interactive read
    from stdin -> /dev/tty (7 sites), prompts to stderr (19 calls)
  - add --from-step=N, --verify, --reset, stale PID detection,
    NodeSource GPG atomic keyring write

2.0.0 - 2.9.0  2026-03-08 - 2026-03-15
  - full rewrite: 19-step automated setup with checkpoint resume
  - add --dry-run, --interactive, --minimal, unattended default
  - add run()/write_file()/write_file_sudo() atomic helpers,
    PID-based mkdir lock, GPU/audio/display/Vulkan verification
  - replace eval with "$@", UID 1000 with $(id -u), apt with apt-get
  - add VM memory tuning; fix OLED rgba=none; remove pulseaudio daemon

1.0.0 - 1.1.0  2026-03-08
  - initial release: Crostini bootstrap with checkpoint resume,
    --dry-run, colored output, full logging, and GPU detection
